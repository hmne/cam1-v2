# 📷 نظام التحكم بالكاميرا عن بُعد - N.S-Cam1

<div dir="rtl">

## 📋 نظرة عامة

**cam1-v2** هو نظام احترافي متطور للتحكم بكاميرات Raspberry Pi عن بُعد. يوفر النظام بثًا مباشرًا، التقاط الصور عالي الدقة، مراقبة الحالة في الوقت الفعلي، وإدارة الأنفاق الآمنة من خلال واجهة ويب سهلة الاستخدام.

### ✨ المميزات الرئيسية

- 📹 **البث المباشر** - بث فيديو مباشر مع خيارات جودة متعددة
- 📸 **التقاط الصور** - التقاط صور عالية الدقة عن بُعد
- 📊 **مراقبة النظام** - إحصائيات فورية (الحرارة، الذاكرة، الشبكة، القرص)
- 🌐 **إدارة الأنفاق** - دعم عدة خوادم (Cloudflared، ngrok، إلخ)
- 🔍 **مراقبة الشبكة** - مراقبة مستمرة للاتصال مع استعادة تلقائية
- ⚙️ **الإدارة عن بُعد** - إعادة التشغيل، الإيقاف، والتنظيف
- 🔌 **نظام الإضافات** - بنية قابلة للتوسع للوظائف المخصصة
- 🔒 **الأمان** - حماية CSRF، التحقق من المدخلات، عمليات ملفات ذرية

---

## 🛠️ التقنيات المستخدمة

### الخلفية (Backend)
- **PHP 8.0+** مع الكتابة الصارمة
- **Bash Scripts** لعمليات النظام والتحكم بالكاميرا
- **raspistill** للتفاعل مع كاميرا Raspberry Pi

### الواجهة الأمامية (Frontend)
- **HTML5** مع تصميم متجاوب
- **CSS3** مع تأثيرات زجاجية (Glassmorphism)
- **JavaScript (ES5+)** مع jQuery 3.7.1
- **AJAX** للتحديثات الفورية

### البنية التحتية
- **Cloudflared** للأنفاق الآمنة
- **SSH/SFTP** للوصول عن بُعد
- **إدارة الحالة المبنية على الملفات** (بدون قاعدة بيانات)

---

## 📦 التثبيت

### المتطلبات الأساسية

**على خادم الويب:**
- PHP 8.0 أو أحدث
- Apache أو Nginx
- صلاحيات الكتابة على المجلدات

**على Raspberry Pi:**
- Raspberry Pi OS
- كاميرا Raspberry Pi
- اتصال إنترنت
- Cloudflared (اختياري)

### خطوات التثبيت

#### 1️⃣ إعداد خادم الويب

```bash
# استنساخ المستودع
git clone https://github.com/your-username/cam1-v2.git /var/www/netstorm.site/cam1

# الانتقال إلى المجلد
cd /var/www/netstorm.site/cam1

# ضبط الصلاحيات
chmod 755 .
chmod 777 tmp/ log/
chmod 644 *.php
```

#### 2️⃣ إعداد Raspberry Pi

```bash
# نسخ السكريبتات من script/ إلى ~/cam/
mkdir -p ~/cam
cp script/sh* ~/cam/

# إضافة امتداد .sh
cd ~/cam
for f in sh*_; do
    mv "$f" "${f%_}.sh"
done

# جعل الملفات قابلة للتنفيذ
chmod +x *.sh

# إضافة إلى crontab للتشغيل التلقائي عند الإقلاع
crontab -e
# أضف السطر التالي:
@reboot /home/pi/cam/boot.sh
```

#### 3️⃣ الإعدادات

افتح `config/app-config.php` وقم بتعديل:

```php
// هوية الكاميرا
define('CAMERA_ID', 'cam1');
define('CAMERA_DISPLAY_NAME', 'N.S-Cam1');
define('CAMERA_BASE_URL', 'http://netstorm.site/cam1');

// إعدادات الأداء
define('SPEED_MODE', false); // true للأداء الأسرع
```

في ملفات Bash (`script/sh*_`):

```bash
readonly DEVICE_ID="cam1"
readonly BASE_URL="http://netstorm.site/cam1"
```

---

## 🚀 الاستخدام

### 1. الوصول للواجهة

افتح المتصفح وانتقل إلى:
```
http://netstorm.site/cam1/index.php
```

### 2. إعدادات الكاميرا

من الواجهة الرئيسية يمكنك ضبط:

- **الدقة (Resolution):**
  - 1280x960
  - 1920x1440
  - 2592x1944
  - 3200x2400

- **الضغط (Compression):**
  - منخفض (25) - أسرع، حجم أكبر
  - متوسط (20)
  - عالي (10)
  - عالي جداً (5) - أبطأ، أفضل جودة

- **معدل الإطارات (FPS):**
  - تلقائي
  - 30، 60، 120، 240، 480، 960

- **إعدادات أخرى:**
  - الصورة (ملونة / رمادية)
  - الدوران (-90°، 0°، 90°، 180°)
  - التأثير (عادي / سلبي)
  - الحدة (عادي / متوسط / عالي)

### 3. البث المباشر

1. اختر **Live** → **Yes**
2. حدد جودة البث:
   - منخفضة جداً - للاتصالات البطيئة جداً
   - منخفضة - للاتصالات البطيئة
   - متوسطة - للاتصالات العادية
   - عالية - للاتصالات السريعة

### 4. التقاط صورة

انقر على زر **Capture** لالتقاط صورة عالية الدقة.

> **ملاحظة:** لا يمكن التقاط صورة أثناء البث المباشر النشط.

### 5. مراقبة الحالة

تعرض الواجهة في الوقت الفعلي:
- حالة الاتصال (متصل/غير متصل)
- آخر وقت وصول
- البيانات المنقولة
- درجة الحرارة
- جودة الإشارة
- زمن الاستجابة

### 6. الوصول عبر الخوادم

عند الاتصال، ستظهر أزرار الخوادم المتاحة:
- **Server 1** (Cloudflared)
- **Server 2** (نفق بديل)
- **Server 3** (نفق بديل)
- **Server 4** (نفق بديل)

### 7. العمليات الإدارية

من الواجهة الرئيسية:
- **🔄 Reboot** - إعادة تشغيل Raspberry Pi
- **⚡ Shutdown** - إيقاف تشغيل النظام
- **🧹 Clear Files** - تنظيف الملفات المؤقتة

---

## 📁 هيكل المشروع

```
cam1-v2/
├── admin/              # نقاط النهاية الإدارية
├── assets/             # الموارد الثابتة (CSS، JS، صور)
├── config/             # ملفات الإعدادات
├── downloads/          # مجلد التحميلات
├── includes/           # الوحدات المشتركة والإضافات
├── log/                # سجلات التطبيق
├── script/             # سكريبتات Bash لـ Raspberry Pi
├── tmp/                # ملفات الحالة الوقتية
├── web/                # موارد ويب إضافية
├── index.php           # الواجهة الرئيسية
├── mode.php            # وحدة عرض الحالة
├── ping.php            # مراقب حالة الشبكة
├── log.php             # عارض السجلات
├── storage.php         # واجهة برمجية موحدة للتخزين
└── buffer.jpg          # مخزن الصور المؤقت
```

---

## 🔐 الأمان

### الميزات الأمنية

1. **حماية CSRF** - رموز جلسة لجميع العمليات الحساسة
2. **التحقق من المدخلات** - قوائم بيضاء وفحص النطاقات
3. **هروب HTML** - جميع المخرجات محمية ضد XSS
4. **عمليات ملفات ذرية** - منع تضارب السباق
5. **رؤوس أمنية** - X-Frame-Options، CSP، إلخ
6. **مسارات آمنة** - منع اختراق المجلدات
7. **السجلات** - تسجيل شامل لجميع الأحداث

### أفضل الممارسات

```php
// ✅ صحيح - استخدام الدوال الآمنة
echo escapeHtml($userInput);
$value = sanitizeInteger($_POST['value'], 0, 100, 50);

// ❌ خطأ - مخرجات غير آمنة
echo $_GET['param']; // ثغرة XSS!
```

---

## 🔧 الإعدادات المتقدمة

### وضع السرعة (SPEED_MODE)

لتحسين الأداء على الاتصالات البطيئة:

```php
// في config/app-config.php
define('SPEED_MODE', true);
```

**الفوائد:**
- صورة خلفية ثابتة (بدون فيديو)
- ملفات CSS/JS مضغوطة
- تحميل صفحات أسرع

### إضافة كاميرا جديدة

1. انسخ المجلد بالكامل:
```bash
cp -r /var/www/netstorm.site/cam1 /var/www/netstorm.site/cam2
```

2. عدّل الإعدادات في `config/app-config.php`:
```php
define('CAMERA_ID', 'cam2');
define('CAMERA_DISPLAY_NAME', 'N.S-Cam2');
define('CAMERA_BASE_URL', 'http://netstorm.site/cam2');
```

### نظام الإضافات

إنشاء إضافة جديدة:

1. أنشئ ملف في `includes/plugins/php/my-plugin.php`:

```php
<?php
if (!isset($GLOBALS['_app_included'])) {
    exit('Direct access not allowed');
}

echo '<div class="plugin-container">';
echo '<h3>إضافتي المخصصة</h3>';
echo '<p>محتوى الإضافة هنا...</p>';
echo '</div>';
?>
```

2. سجّل الإضافة في `includes/plugins/manifest.json`:

```json
{
    "name": "my-plugin",
    "type": "php",
    "file": "php/my-plugin.php",
    "enabled": true,
    "priority": 10,
    "description": "إضافة مخصصة للنظام"
}
```

---

## 📊 المراقبة والسجلات

### السجلات المتاحة

- **log/log.txt** - أحداث التطبيق الرئيسية
- **log/ping.txt** - مراقبة الشبكة
- **log/combined.log** - جميع سجلات النظام
- **log/sync.log** - عمليات مزامنة الملفات
- **log/tunel*.log** - سجلات الأنفاق
- **log/php_errors.log** - أخطاء PHP

### عرض السجلات

من الواجهة:
```
http://netstorm.site/cam1/log.php?file=log.txt&lines=200
```

من سطر الأوامر:
```bash
tail -f log/log.txt
```

### مراقبة الشبكة

للوصول لمراقب الشبكة المتقدم:
```
http://netstorm.site/cam1/ping.php
```

الميزات:
- إحصائيات النظام في الوقت الفعلي
- سجل الاتصال مع رموز ملونة
- تحديث تلقائي تكيفي
- تحميل السجل

---

## 🐛 استكشاف الأخطاء

### المشكلة: "الكاميرا غير متصلة"

**الأسباب المحتملة:**
- `tmp/status.tmp` غير موجود أو قديم (>7 ثواني)
- `script/shmonitor_` غير قيد التشغيل على Pi
- مشاكل في الاتصال بالشبكة

**الحل:**
```bash
# على Raspberry Pi
ps aux | grep shmonitor
# إذا لم يكن قيد التشغيل:
~/cam/monitor.sh &
```

### المشكلة: البث المباشر لا يعمل

**الحل:**
1. تحقق من `tmp/web_live.tmp` = 'on'
2. تأكد من تشغيل `script/shlive_` على Pi
3. تحقق من عمل مزامنة SFTP
4. تأكد من إنشاء `live.jpg`

### المشكلة: أخطاء في الصلاحيات

**الحل:**
```bash
# ضبط صلاحيات المجلدات
chmod 777 tmp/ log/

# أو بشكل أكثر أماناً:
chown www-data:www-data tmp/ log/
chmod 775 tmp/ log/
```

### المشكلة: استهلاك عالٍ للمعالج

**الحل:**
- زيادة فترة التحديثات في `camera-control.js`
- تفعيل `SPEED_MODE`
- تقليل جودة البث المباشر

---

## 🔄 التحديثات المستقبلية

### قيد التطوير

- [ ] دعم كاميرات متعددة في واجهة واحدة
- [ ] تسجيل الفيديو
- [ ] كشف الحركة
- [ ] إشعارات الهاتف المحمول
- [ ] تطبيق موبايل (Android/iOS)
- [ ] تحليل الصور بالذكاء الاصطناعي
- [ ] التخزين السحابي التلقائي

---

## 🤝 المساهمة

نرحب بالمساهمات! يرجى اتباع الخطوات التالية:

1. Fork المستودع
2. إنشاء فرع للميزة: `git checkout -b feature/amazing-feature`
3. Commit التغييرات: `git commit -m 'إضافة ميزة رائعة'`
4. Push للفرع: `git push origin feature/amazing-feature`
5. فتح Pull Request

### معايير الكود

- **PHP:** اتبع PSR-12
- **JavaScript:** استخدم ES5+ مع JSDoc
- **Bash:** اتبع أفضل ممارسات Shell

---

## 📄 الترخيص

هذا المشروع محمي بحقوق الملكية - جميع الحقوق محفوظة © 2025 Net Storm

---

## 👨‍💻 المطور

**Net Storm**

- 📧 البريد الإلكتروني: [البريد الإلكتروني]
- 🌐 الموقع: [الموقع الإلكتروني]
- 💼 GitHub: [@your-username]

---

## 🙏 شكر وتقدير

- **Raspberry Pi Foundation** - للأجهزة الرائعة
- **Cloudflare** - لخدمة Cloudflared
- **jQuery Team** - للمكتبة الممتازة
- **المجتمع** - للدعم والمساهمات

---

## 📞 الدعم

إذا واجهت أي مشاكل:

1. تحقق من السجلات في `log/`
2. راجع قسم [استكشاف الأخطاء](#-استكشاف-الأخطاء)
3. افتح Issue على GitHub
4. راجع [CLAUDE.md](CLAUDE.md) للتوثيق التقني التفصيلي

---

<div align="center">

**صُنع بـ ❤️ للمراقبة عن بُعد**

⭐ إذا أعجبك هذا المشروع، لا تنسى إعطاءه نجمة!

</div>

</div>
